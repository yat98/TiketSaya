<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chandra.hidayat.tiketsaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.KeyboardSoft">
        <activity android:name=".activity.ProfileEditActivity"></activity>
        <activity android:name=".activity.DetailTicketBuyActivity" />
        <activity android:name=".activity.ProfileActivity" />
        <activity android:name=".activity.SuccessBuyTicketActivity" />
        <activity android:name=".activity.CheckoutActivity" />
        <activity android:name=".activity.DetailActivity" />
        <activity android:name=".activity.DashboardActivity" />
        <activity android:name=".activity.SuccessSignUpActivity" />
        <activity android:name=".activity.NewAccountProfileActivity" />
        <activity android:name=".activity.NewAccountActivity" />
        <activity android:name=".activity.SignInActivity" />
        <activity android:name=".activity.GetStartedActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>